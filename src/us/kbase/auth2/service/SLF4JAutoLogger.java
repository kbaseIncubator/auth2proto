package us.kbase.auth2.service;

/** Represents a logger that intercepts and logs SLF4J log events. No
 * interface is necessary - just log in the normal way through SLF4J.
 * 
 * Keep a reference to the object so that the auto logger is not garbage
 * collected.
 * @author gaprice@lbl.gov
 *
 */
public interface SLF4JAutoLogger {
	//no interface
}
